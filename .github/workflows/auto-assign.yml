name: Auto Assign PR
on:
  pull_request:
    types: [opened]
jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
    - name: 'Auto-assign PR'
      uses: kentaro-m/auto-assign-action@v2.0.2